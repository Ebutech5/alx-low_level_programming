#include <stdio.h>

/* betty style for doc function goes there */
/**
*  main - main function
* Return: Always 0 (Success)
*/

int main(void)
{
	char i;

	for (i = 'a' ; i <= 'z' ; i++)
		putchar(i);
	putchar('\n');
	return (0);
}
