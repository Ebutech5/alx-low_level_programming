#include "main.h"

/**
* _isalpha - writes a function that checks alphabetic character
* @c: function parameter
* Return: Always 0 (Success)
*/

int _isalpha(int c)
{
	if ((c >= 'a' && c <= 'z') || ((c >= 'A' && <= 'Z'))
			return (1);
	else
			return (0);
}
